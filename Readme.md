`sudo apt-get install git supervisor python3 nginx libjpeg-dev`

`
curl -sL https://deb.nodesource.com/setup_6.x | sudo bash -
sudo apt-get install nodejs
npm install bower -g
bower install ngtouch --save
`

